---
import BaseLayoutCal from '../../layouts/BaseLayoutCal.astro';
---

<BaseLayoutCal>

<section class="calc-hero">
    <h1 class="calc-titulo">CALCULADORAS</h1>
    <p class="calc-subtitulo">
        Del papel a la lógica digital. Explora las dimensiones del espacio con nuestras herramientas interactivas.
    </p>
</section>

<div class="sobre-wrapper">
    <h2 class="titulo-sobre">SOBRE EL PROYECTO</h2>
    <img class="robot-boceto" src="/proyects/imagenes/calculadoras/LERPparado.png" alt="Robot ELOA">
</div>

<div class="container_proyecto">
    <h3>Herramientas creadas para automatizar y facilitar las operaciones de conversión y cálculo de distancias utilizando coordenadas. A través de estas herramientas, podrás realizar diferentes cálculos de manera rápida y precisa, evitando procesos manuales que pueden resultar complicados o propensos a errores. Además, podrás visualizar y comprender mejor la relación entre las coordenadas, sus unidades de medida y las distancias obtenidas, haciendo que el proceso sea más sencillo y práctico para ti.</h3>
    <a class="play-btn" aria-label="Reproducir" href="https://youtu.be/EFhqvBubqGQ?si=NI324KUQ5yE1HSQm" target="_blank" rel="noopener noreferrer">
    <span class="play-triangulo"></span>
</a>
</div>

<div class="galeria">
    <img class="ilustracion" src="/proyects/imagenes/calculadoras/nosotros.png" alt="equipo">
</div>

</BaseLayoutCal>

<style>
*,
*::before,
*::after{
    box-sizing: border-box; /* evita que el padding sume ancho extra y cause overflow-x */
}

html{
    overflow-x: hidden;
}

body{
    background-image: url('/proyects/imagenes/calculadoras/fondocalculadoras.png');
    overflow-x: hidden; /* oculta lo que sale horizontalmente (izq-der) de un contendor*/
    margin: 0;
    padding: 0;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-attachment: fixed; 
    width: 100%;
    min-height: 100vh; 
}

/* ===== MOBILE FIRST ===== */

.calc-hero{
    padding: 110px 20px 20px 20px;
    text-align: center;
}

.calc-titulo{
    font-family: 'Oswald', 'Arial Narrow', Arial, sans-serif;
    letter-spacing: 2px;
    font-size: clamp(30px, 6vw, 78px);
    color: white;
    text-shadow: 2px 2px 6px rgba(0,0,0,0.5);
    margin: 0;
    text-transform: uppercase;
    padding: 20px;
}

.calc-subtitulo{
    font-family: 'Mulish', 'Lucida Sans', Verdana, sans-serif;
    color: white;
    font-size: clamp(15px, 3.5vw, 25px);
    text-shadow: 2px 2px 6px rgba(0,0,0,0.4);
    text-align: center;
    margin: 15px 0 0 0;
    line-height: 1.5;
}

/* Contenedor del bloque "sobre el proyecto": el robot va arriba
   pegado a la derecha, y el título debajo, centrado (igual que el mockup,
   en cualquier tamaño de pantalla) */
.sobre-wrapper{
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 10px  50px 20px;
    gap: clamp(10px, 1.5vw, 45px);
    overflow: visible; /* para que las antenas del robot no se recorten */
    max-width:100%;
    margin-left: auto;
    margin-right: auto;
}

.titulo-sobre{
    font-family: 'Oswald', 'Arial Narrow', Arial, sans-serif;
    letter-spacing: -1px;
    font-size: clamp(26px, 5vw, 56px);
    color: #182d42;
    text-align: center;
    margin: 0;
    order: 2; /* siempre va DESPUÉS del robot */
}

/* El robot vive en el flujo normal (no absolute) para que su alto real
   nunca se salga de su espacio sin importar la proporción de la imagen.
   El ancho escala con la pantalla; el alto queda libre para respetar
   su proporción real. */
.robot-boceto{
    display: block;
    width: clamp(190px, 20vw, 340px);
    height: auto;
    order: 1; /* siempre va PRIMERO, arriba del título */
    align-self: flex-end; /* pegado a la derecha, como en el mockup */
    margin-right: -6px; /* leve sangría hacia el borde */
}

.container_proyecto{
    width: 100%;
    background-color: #182d42;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: clamp(20px, 3vw, 50px) 20px clamp(90px, 8vw, 140px) 20px;
    margin-top: 15px;
    position: relative;
}

h3{
    line-height: 1.6;
    margin: 0 auto;
    letter-spacing: 0.3px;
    width: 100%;
    max-width: min(90vw, 900px);
    text-align: center;
    text-align:justify;
    font-size: clamp(16px, 1.6vw, 24px);
    font-weight: 600;
    font-family: 'Mulish', 'Lucida Sans', Verdana, sans-serif;
    color: #ffffff;
}

.play-btn{
    position: absolute;
    bottom: clamp(15px, 2vw, 30px);
    right: clamp(20px, 3vw, 50px);
    width: clamp(42px, 4vw, 60px);
    height: clamp(42px, 4vw, 60px);
    border-radius: 50%;
    background-color: rgba(255,255,255,0.15);
    border: 2px solid white;
    color: white;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    appearance: none;
    -webkit-appearance: none;
    padding: 0;
    z-index: 5;
}

.play-triangulo{
    display: block;
    width: 0;
    height: 0;
    border-top: 7px solid transparent;
    border-bottom: 7px solid transparent;
    border-left: 11px solid white;
    margin-left: 3px; /* centra visualmente el triángulo dentro del círculo */
}

.galeria{
    padding: 0 20px 40px 20px;
    margin-top: clamp(-100px, -9vw, -60px); /* sube TODO este bloque para encimarlo con claridad sobre el azul */
    display: flex;
    justify-content: center;
    position: relative;
    z-index: 3;
}

.ilustracion{
    display: block;
    width: 100%;
    max-width: clamp(220px, 22vw, 400px);
    border-radius: 6px;
    box-shadow: 0 4px 14px rgba(0,0,0,0.3);
    z-index: 4;
}
</style>