<footer class="footer-eloa">
  <div class="arco arco-izquierdo"></div>
  <div class="arco arco-derecho"></div>

  <div class="redes-sociales">
    <a href="https://www.instagram.com/eloadevteam" target="_blank" rel="noopener noreferrer">
        <img class="imagenes_redes" src="/proyects/imagenes/inicio/instagram.png" alt="Instagram" />
    </a>
        <img class="imagenes_redes" src="/proyects/imagenes/inicio/correo.png" alt="Correo" />
  </div>

  <div class="footer-contenido">
    
    <div class="footer-header">
      <div class="logo-principal">
        <img src="/proyects/imagenes/inicio/logo1FINAL.png" alt="ELOA Dev Team" class="img-logo-eloa">
    </div>
      <div class="linea-separadora"></div>
      
      <div class="slogan-art">
        <p>
          <span class="texto-oscuro">E a t</span> <span class="texto-naranja">L o g i c</span><br>
          <span class="texto-oscuro">O u t p u t</span> <span class="texto-morado">A r t</span>
        </p>
      </div>
    </div>

    <div class="footer-columnas">
      
      <div class="columna-info1">
        <p class="texto-normal">Hermosillo, Sonora<br>eloadevteam@gmail.com</p>
        <h4 class="titulo-naranja">Tus ideas no solo se<br>escuchan, aquí se<br>construyen.</h4>
      </div>

      <div class="columna-info2">
        <h4 class="titulo-oscuro">Aprovechamos este espacio para<br>reconocer a</h4>
        <img src="/proyects/imagenes/inicio/logo-unison.png" alt="Universidad de Sonora" class="logo-unison">
        
        <div class="profesor-seccion">
          <p class="texto-normal">A nuestro profesor encargado: Jose Alfredo Galaviz Sañudo</p>
        </div>
      </div>

    </div>

    <div class="footer-copyright">
      <p><span class="texto-naranja">© 2026</span> Trabajo realizado por alumnos de la Universidad de Sonora</p>
    </div>

  </div>
</footer>

<style>
/* FOOTER*/
    :root {
    --azul-oscuro: #1b2e45;
    --naranja: #d46b42;
    --morado: #b886b5;
    --azul-claro: #83a5b6;
    }

    .footer-eloa {
    position: relative;
    z-index: 0; /* crea su propio contexto de apilamiento: sin esto, los z-index de
                   .redes-sociales y .footer-contenido "se escapan" del footer y
                   compiten contra CUALQUIER otro elemento de la página (como un
                   popup), en vez de quedar contenidos aquí adentro */
    overflow: hidden; 
    background-color: #ffffff;
    padding: 40px 0 20px 0;
    font-family: 'Montserrat', sans-serif;
    color: var(--azul-oscuro);
    }

    /* ===== MOBILE FIRST: base para pantallas chicas ===== */

    /* arcos: se achican mucho para que no dominen la pantalla en mobile */
    .arco {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 220px;
    height: 220px;
    border-radius: 50%;
    border: 15px solid var(--azul-claro);
    z-index: 1; 
    }
    .arco-izquierdo { 
        left: -140px; 
    }
    .arco-derecho { 
        right: -140px; 
        top:60px
    }

    /* redes: en mobile ya no van position:absolute (se encimarían), van arriba del contenido en fila */
    .redes-sociales {
    position: static;
    transform: none;
    display: flex;
    flex-direction: row;
    justify-content: center;
    gap: 18px;
    margin-bottom: 20px;
    z-index: 10;
    }
    .redes-sociales img {
    width: 28px;
    height: 28px;
    transition: transform 0.2s ease;
    }
    .redes-sociales img:hover {
    transform: scale(1.1); 
    }

    
    .footer-contenido {
    position: relative;
    z-index: 5; 
    max-width: 900px;
    margin: 0 auto; 
    padding: 0 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    }

    
    .footer-header {
    display: flex;
    flex-direction: column; /* logo arriba, slogan abajo en mobile */
    align-items: center;
    gap: 15px;
    margin-bottom: 30px;
    text-align: center;
    }
    .logo-principal {
    display: flex;
    align-items: center;
    justify-content: center;
    }

    .img-logo-eloa {
    max-height: 60px;  
    width: auto;       
    object-fit: contain;
    }
    .linea-separadora {
    width: 50px;   /* horizontal en vez de vertical en mobile */
    height: 2px;
    background-color: var(--azul-oscuro);
    }
    .slogan-art p {
    font-size: 22px; /* mucho más chico que los 40px originales */
    font-weight: 900;
    line-height: 1.3;
    margin: 0;
    }
    .texto-oscuro { color: var(--azul-oscuro); }
    .texto-naranja { color: var(--naranja); }
    .texto-morado { color: var(--morado); }

   
    .footer-columnas {
    display: flex;
    flex-direction: column; /* columnas apiladas en mobile */
    justify-content: center;
    width: 100%;
    padding: 0;
    gap: 30px;
    }
    .columna-info1{
    flex: 1;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    padding: 10px;
    }

    .columna-info2 {
    flex: 1;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 5px;
    }

    .titulo-oscuro {
    font-size: 15px;
    font-weight: 700;
    margin: 0;
    color: #182d42;
    }
    .titulo-naranja {
    font-size: 15px;
    font-weight: 700;
    color: var(--naranja);
    margin: 0;
    }
    .texto-normal {
    font-size: 14px;
    font-weight: 400;
    margin: 0;
    line-height: 1.6;
    }
    .logo-unison {
    max-width: 180px;
    height: auto;
    }

    .profesor-seccion {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 15px;
    margin-top: 10px;
    }

    
    .footer-copyright {
    margin-top: 20px;
    text-align: center;
    font-size: 12px;
    color: #666;
    }

    /* ===== A partir de aquí, pantallas más grandes (tablet/escritorio) ===== */
    @media (min-width: 700px) {

        .footer-eloa {
            padding: 60px 0 20px 0;
        }

        .arco {
            width: 500px;
            height: 500px;
            border: 30px solid var(--azul-claro);
        }
        .arco-izquierdo { left: -300px; }
        .arco-derecho { right: -300px; top: 100px; }

        .redes-sociales {
            position: absolute;
            left: 40px;
            top: 50%;
            transform: translateY(-50%);
            flex-direction: column;
            gap: 20px;
            margin-bottom: 0;
        }
        .redes-sociales img {
            width: 40px;
            height: 40px;
        }

        .footer-header {
            flex-direction: row;
            gap: 40px;
            margin-bottom: 50px;
        }

        .img-logo-eloa {
            max-height: 100px;
        }

        .linea-separadora {
            width: 2px;
            height: 90px;
        }

        .slogan-art p {
            font-size: 40px;
        }

        .footer-columnas {
            flex-direction: row;
            justify-content: space-between;
            padding: 0 50px;
            gap: 50px;
        }

        .columna-info1 {
            gap: 30px;
            padding: 20px;
        }

        .titulo-oscuro { font-size: 18px; }
        .titulo-naranja { font-size: 20px; }
        .texto-normal { font-size: 15px; }

        .logo-unison { max-width: 270px; }

        .profesor-seccion {
            flex-wrap: nowrap;
            gap: 30px;
        }

        .footer-copyright {
            font-size: 14px;
        }
    }
</style>